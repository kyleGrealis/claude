# Claude Code Runtime & Cache Files
# Keep agents, skills, CLAUDE.md, and settings.json in version control
# Ignore everything else

# Credentials - NEVER COMMIT!
.credentials.json
*.credentials.json

# Settings that should remain local (if needed later)
# settings.local.json

# Claude Code runtime directories
cache/
debug/
downloads/
file-history/
ide/
paste-cache/
plans/
projects/
session-env/
shell-snapshots/
statsig/
telemetry/
todos/

# Command history
history.jsonl

# Plugins (may have machine-specific state)
plugins/
